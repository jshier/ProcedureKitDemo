platform :ios, '10.0'

target 'ProcedureKitDemo' do
  use_frameworks!

  pod 'ProcedureKit', :git => 'https://github.com/ProcedureKit/ProcedureKit.git', :branch => 'development'

  target 'ProcedureKitDemoTests' do
    inherit! :search_paths
  end
end
